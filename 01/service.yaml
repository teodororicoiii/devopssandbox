apiVersion: v1
kind: Service
metadata:
  labels:
    app: thinknyx
  name: thinknyx
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: thinknyx
  type: LoadBalancer
status:
  loadBalancer: {}
